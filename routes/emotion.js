const express = require("express");
const router = express.Router();
// const { someEmotionController } = require("../server/controllers/emotion"); // Uncomment and update when controller exists
const auth = require("../server/middlewares/auth");

// TODO: Add emotion-related routes here
// Example:
// router.post("/log", auth, someEmotionController);

module.exports = router;
